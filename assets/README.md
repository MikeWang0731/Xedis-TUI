# 产品截图

将产品截图放在此目录，例如 `workspace.webp`。
在 `index.html` 中搜索 `SCREENSHOT`，按照旁边注释将整个 `.screenshot-placeholder` 元素替换为 `<img>`。
推荐 1600 × 900 或更高分辨率，保留真实产品界面；`product-screenshot` 会自动适配宽度。
图片使用相对路径 `./assets/workspace.webp`，兼容 GitHub Pages 项目子路径。
